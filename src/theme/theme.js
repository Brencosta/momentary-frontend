import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#007bff",
    accent: "#ff0000",
    background: "#ffffff",
    surface: "#ffffff",
    text: "#000000",
    placeholder: "#888888",
  },
};

export default theme;
